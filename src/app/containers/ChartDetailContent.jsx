import React from "react"
import '../../../public/styles/layouts.scss'

const ChartDetailContent = ({children}) => {
    return (
        <section className="ChartDetailContent">
            {children}
        </section>
    )
}

export default ChartDetailContent